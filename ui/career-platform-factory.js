"use strict";

/* Angular factory to interact with the CareerPlatform APIs */
angular.module("myApp").factory("careerPlatformFactory", ["$http", "$q", function ($http, $q) {
    var factory = {};
    return factory;
}]);